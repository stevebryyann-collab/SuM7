import { ComingSoon } from '@/components/merchant/ComingSoon';

/** Merchant settings (general / invoice / notifications) — full build in Group 4. */
export default function SettingsPage(): JSX.Element {
  return (
    <ComingSoon
      title="Settings"
      description="General, invoice, and notification preferences."
    />
  );
}
