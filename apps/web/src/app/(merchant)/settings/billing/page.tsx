import { ComingSoon } from '@/components/merchant/ComingSoon';

/** Billing & subscription — plan card, usage, Stripe portal — full build in Group 4. */
export default function BillingSettingsPage(): JSX.Element {
  return (
    <ComingSoon
      title="Billing"
      description="Your plan, GMV usage, and payment method."
    />
  );
}
