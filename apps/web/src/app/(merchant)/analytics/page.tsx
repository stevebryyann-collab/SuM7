import { ComingSoon } from '@/components/merchant/ComingSoon';

/** Analytics dashboard — full implementation lands in Group 4. */
export default function AnalyticsPage(): JSX.Element {
  return (
    <ComingSoon
      title="Analytics"
      description="GMV, orders, and top buyers over time."
    />
  );
}
