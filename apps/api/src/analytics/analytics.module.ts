import { Module } from '@nestjs/common';

/**
 * Merchant analytics (GMV, AR aging, buyer activity) surfaced via GraphQL.
 * Bootable scaffold — resolvers/services added by the Analytics feature task.
 */
@Module({})
export class AnalyticsModule {}
